# Contentful Delivery API credentials.
# Space ID: Contentful → Settings → General settings
# Token:    Contentful → Settings → API keys → Content Delivery API access token
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=

# Optional — only set this if the content type ID differs from the default.
# CONTENTFUL_PROJECT_TYPE_ID=projects
